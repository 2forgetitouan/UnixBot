<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title || 'Connexion' %> - AxonBot</title>
  <link rel="stylesheet" href="/css/style.css">
  <!-- Favicon (theme-aware SVG) -->
  <link rel="icon" type="image/svg+xml" href="/favicons/1light.svg" media="(prefers-color-scheme: light)">
  <link rel="icon" type="image/svg+xml" href="/favicons/1dark.svg" media="(prefers-color-scheme: dark)">
  <link rel="icon" type="image/svg+xml" href="/favicons/1dark.svg">
  <style>
    body {
      background: radial-gradient(circle at 20% 20%, rgba(99,102,241,0.08), transparent 25%),
                  radial-gradient(circle at 80% 0%, rgba(56,189,248,0.06), transparent 25%),
                  #0b1021;
      color: #e5e7eb;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 24px;
      font-family: 'Inter', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
    }
    .card {
      width: 100%;
      max-width: 480px;
      background: rgba(15, 23, 42, 0.9);
      border: 1px solid rgba(248, 113, 113, 0.3);
      border-radius: 14px;
      padding: 26px;
      box-shadow: 0 20px 60px rgba(0,0,0,0.4);
      backdrop-filter: blur(6px);
    }
    .header {
      display: flex;
      align-items: center;
      gap: 12px;
      margin-bottom: 16px;
    }
    .title {
      font-size: 1.35rem;
      font-weight: 700;
      margin: 0;
      color: #fecdd3;
    }
    .reason {
      margin: 0 0 14px;
      color: #fda4af;
      line-height: 1.5;
    }
    .cta {
      display: inline-flex;
      align-items: center;
      gap: 10px;
      background: linear-gradient(120deg, #4b5563, #1f2937);
      color: #f8fafc;
      border: 1px solid rgba(255,255,255,0.1);
      border-radius: 10px;
      padding: 12px 16px;
      font-weight: 700;
      text-decoration: none;
      box-shadow: 0 10px 30px rgba(0,0,0,0.35);
      transition: transform 0.12s ease, box-shadow 0.12s ease;
    }
    .cta:hover {
      transform: translateY(-1px);
      box-shadow: 0 14px 40px rgba(0,0,0,0.45);
    }
    .cta:active { transform: translateY(0); }
    .muted {
      margin-top: 14px;
      color: #cbd5e1;
      font-size: 0.95rem;
      line-height: 1.4;
    }
    .chip {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      background: rgba(99, 102, 241, 0.12);
      border: 1px solid rgba(99, 102, 241, 0.35);
      border-radius: 999px;
      padding: 6px 12px;
      color: #e0e7ff;
      font-weight: 600;
      margin-top: 6px;
    }
  </style>
</head>
<body>
  <div class="card">
    <div class="header">
      <img src="/favicon.png" alt="AxonBot" width="36" height="36" style="border-radius:8px;">
      <h1 class="title"><%= title || 'Connexion refusée' %></h1>
    </div>
    <p class="reason"><%= reason || 'La connexion a échoué.' %></p>
    <a class="cta" href="/auth/login">↩ Retour à la connexion</a>
    <div class="muted">
      En cas de problème, veuillez contacter :
      <div class="chip">👤 deleted_user_562</div>
    </div>
  </div>
</body>
</html>
